build/
*.o
*.exe
*.a
*.dll
*.so
# generated RV32I test binaries: built from source into build/ or in place by
# the sub-Makefiles, and published by CI as build artifacts, never committed
*.elf
*.bin
# editor swap files and clang static-analyzer output (see "make sanitize" / static-analysis workflow)
*.sw?
*.plist
